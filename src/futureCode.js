//      Function for getting the top ten tips from the entire tip array

//    const getTopTenData = (tips) => {
//       const sortedTips = tips.sort((a, b) => (a.tip_total > b.tip_total) ? -1 : 1)
//       const topTen = sortedTips.slice(0, 10)
//     };
